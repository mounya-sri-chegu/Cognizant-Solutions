const eventName = "Art Fair";
const eventDate = "2025-06-20";
let seats = 50;

const eventInfo = `${eventName} is on ${eventDate}. Seats available: ${seats}`;
console.log(eventInfo);

seats--;
console.log(`Seats remaining: ${seats}`);